---
name: CI

# yamllint disable-line
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run yamllint
        run: yamllint .
      - name: Run kubeconform
        uses: docker://ghcr.io/yannh/kubeconform:v0.4.13
        with:
          entrypoint: /kubeconform
          args: "-kubernetes-version 1.23.8 -summary -ignore-missing-schemas -ignore-filename-pattern .github ."
